/**
 * Template for local Finnhub credentials.
 * Copy to `config.js` (gitignored) and set a real key — `script.js` reads
 * `window.FINNHUB_API_KEY` at load time.
 * SECURITY: never commit a real key; keep secrets in untracked `config.js` only.
 */
window.FINNHUB_API_KEY = "";
